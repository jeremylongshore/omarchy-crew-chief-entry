{
  "name": "omarchy-crew-chief",
  "version": "1.0.0",
  "private": true,
  "description": "Claude Code fleet attention router for the Omarchy bar - dev harness for Model.js and the hook script",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
